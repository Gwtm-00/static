pipeline {
     agent any
     stages {
         stage('Build') {
             steps {
                 sh 'echo "Working"'
                 sh '''
                     echo "Multiline shell steps works too"
                     ls -lah
                 '''
             }
         }
     }
}